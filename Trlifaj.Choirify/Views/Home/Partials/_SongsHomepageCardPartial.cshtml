<div class="col-md-4">
    <div class="text-center homepage-menu-item-container">
        @if (User.Identity.IsAuthenticated && User.IsInRole(Roles.Singer))
        {
            <a class="homepage-menu-link" asp-area="" asp-controller="Songs">
                <i class="fas fa-music fa-8x"></i>
                <h2>Skladby</h2>
            </a>
        }
        else if (User.Identity.IsAuthenticated && User.IsInRole(Roles.Choirmaster))
        {
            <a class="homepage-menu-link" asp-area="" asp-controller="Songs" asp-action="Choirmaster">
                <i class="far fa-music fa-8x"></i>
                <h2>Skladby</h2>
            </a>
        }
        else
        {
            <i class="fas fa-music fa-8x"></i>
            <h2>Skladby</h2>
        }
    </div>
</div>